package com.cs.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.cs.entity.Ga4Event;

/**
 * GA4 事件 Mapper
 * MyBatis-Plus BaseMapper 提供了通用 CRUD 方法
 *
 * @author LivePulse
 */
public interface Ga4EventMapper extends BaseMapper<Ga4Event> {
    // BaseMapper 已提供: insert, deleteById, updateById, selectById, selectList, selectCount 等
}