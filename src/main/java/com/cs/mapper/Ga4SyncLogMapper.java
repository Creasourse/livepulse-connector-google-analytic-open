package com.cs.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.cs.entity.Ga4SyncLog;

/**
 * GA4 同步日志 Mapper
 * MyBatis-Plus BaseMapper 提供了通用 CRUD 方法
 *
 * @author LivePulse
 */
public interface Ga4SyncLogMapper extends BaseMapper<Ga4SyncLog> {
    // BaseMapper 已提供: insert, deleteById, updateById, selectById, selectList, selectCount 等
}