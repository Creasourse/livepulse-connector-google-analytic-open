package com.cs.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.cs.entity.Ga4Property;

/**
 * GA4 属性 Mapper
 * MyBatis-Plus BaseMapper 提供了通用 CRUD 方法
 *
 * @author LivePulse
 */
public interface Ga4PropertyMapper extends BaseMapper<Ga4Property> {
    // BaseMapper 已提供: insert, deleteById, updateById, selectById, selectList, selectCount 等
}